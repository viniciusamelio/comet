{
  "name": "comet-cloudflare-worker-example",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "RUSTC=\"$(rustup which rustc)\" \"$(rustup which cargo)\" check --target wasm32-unknown-unknown",
    "test": "cargo test --lib",
    "test:integration": "bash tests/integration.sh",
    "test:perf": "bash tests/perf.sh"
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "wrangler": "4.106.0"
  }
}
